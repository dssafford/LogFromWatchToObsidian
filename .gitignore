# Runtime state (health once-per-day guard)
.health_last_run.json

# Editor/patch backups
*.bak
*.bak-*
.env
oura_secrets.json
oura_tokens.json

# Runtime state (Oura once-per-day retry guard)
.oura_last_run.json
